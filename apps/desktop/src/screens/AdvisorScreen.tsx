/**
 * AdvisorScreen — Router 入口，转交到 modules/advisor/AdvisorView
 * 灵犀演示 · Phase 1 · T-1.2
 */
import React from 'react';
import AdvisorView from '../modules/advisor';

const AdvisorScreen: React.FC = () => {
  return <AdvisorView />;
};

export default AdvisorScreen;
